server.port: 8081
spring:
  main:
    allow-bean-definition-overriding: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://embrave.eu.auth0.com/
  datasource :
    url: jdbc:mysql://localhost:3306/
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update

auth0:
  audience: https://embrave.app

debug: true
logging:
  level:
    org:
      springframework:
        security: DEBUG


